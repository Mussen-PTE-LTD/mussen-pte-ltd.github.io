{% for post in posts_collate  %}
              <div id="{{ post.category }}" class="thumbnail col-xs-6 col-md-4">
                <img src="{{ post.image }}" alt="link to {{ post.url }}">
                <div class="caption">
                  <blockquote>{{ post.description }}</blockquote>
                  {{ post.content }}
                </div>
              </div>
{% endfor %}
{% assign posts_collate = nil %}
